[package]
name = "loczwasm"
version = "0.1.0"
authors = ["ykS"]
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
wasm-bindgen = "0.2.83"
web-sys = { version = "0.3.60", features = ['CanvasRenderingContext2d', 'ImageData'] }
loczlib = { path = "../loczlib" }